## 1. Backend

- [x] 1.1 `GET /api/drive/export` (access-checked bundle with topic paths)
- [x] 1.2 `POST /api/drive/import` (caps + schema validation + topic resolve + create)
- [x] 1.3 `POST /api/drive/folders/[id]/copy` (deep copy, caps)

## 2. Frontend

- [x] 2.1 jszip dep; zip build/parse helpers; single-file download
- [x] 2.2 Import modal (pick, preview, sample, confirm) + ribbon/menu entries
- [x] 2.3 Menus: folder Download/Copy/Import-here, file Download
- [x] 2.4 Tests + tsc + `npm test` green
